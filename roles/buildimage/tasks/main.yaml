---
- name: Get namespace
  ansible.builtin.command: "/usr/local/bin/kubectl get namespace"
  register: namespace
  changed_when: false

- name: Debug
  ansible.builtin.debug:
      var: namespace


# - name: Test get pod
#   ansible.builtin.command: sudo kubectl get po -A
#   changed_when: ture
